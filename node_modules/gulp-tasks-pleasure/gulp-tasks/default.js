/**
 * @file
 * Task: Default.
 */

var shell = require('gulp-shell');

 /* global module */

module.exports = function (gulp, plugins, options) {
  'use strict';

  gulp.task('default',gulp.parallel([shell.task(['python src/assets/bases-de-datos/api.py']),gulp.series('serve')]))
};
