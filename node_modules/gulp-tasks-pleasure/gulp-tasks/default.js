/**
 * @file
 * Task: Default.
 */

var shell = require('gulp-shell');

 /* global module */

module.exports = function (gulp, plugins, options) {
  'use strict';

  gulp.task('default',gulp.parallel([shell.task(['python api.py']),gulp.series('serve')]))
};
