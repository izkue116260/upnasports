/**
 * @file
 * Task: Optimize images.
 */

var shell = require('gulp-shell');

module.exports = function (gulp, plugins, options) {
  'use strict';

  gulp.task('python', shell.task([' python api.py']));
};
